# The role is valid; the defect is in main.py, where the entry function name
# does not match the directory id name_mismatch.
role: transform   # pass-through transform: reads its input, writes it back
