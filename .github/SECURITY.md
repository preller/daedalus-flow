# Security

Do not open a public issue for a vulnerability. Report it through GitHub's
private vulnerability reporting: the repository's Security tab, then "Report a
vulnerability".

A useful report gives the version (`dae --version`), the operating system and
Python version, a minimal way to reproduce the problem, and any traceback or
input that triggers it.

Reports must reproduce; unverified tool output is closed.
