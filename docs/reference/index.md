# Reference

The precise surface of `daedalus-flow`, for looking things up. The CLI page is
generated from the real `dae` app and the Python API from the `daedalus.flow`
source, so neither can drift from the code. The field, outcome-code, and
envelope references describe exactly what the tool reads and writes.

```{toctree}
:maxdepth: 1
cli
lab-yaml-reference
json-envelope
outcome-codes
/api/daedalus/flow/index
```
